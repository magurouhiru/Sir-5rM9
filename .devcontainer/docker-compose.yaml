services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    environment:
      - APP_ENV=development
    secrets:
      - DISCORD_TOKEN
    volumes:
      - ..:/home/vscode/app

secrets:
  DISCORD_TOKEN:
    file: ../discord_token.txt
