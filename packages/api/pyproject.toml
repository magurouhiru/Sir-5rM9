[project]
name = "api"
version = "0.1.0"
description = "ocr server とのapi を定義するパッケージ"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pydantic>=2.12.5"]

[dependency-groups]
dev = ["ruff>=0.15.6"]

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
